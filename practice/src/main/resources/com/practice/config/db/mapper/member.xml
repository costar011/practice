<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">

	<insert id="signup" parameterType="memberVO">
		INSERT INTO MEMBER 
		(NAME, MOBLIE, HP, ZIP_CODE, ADDRESS, ADDRESS2, SEX, ID, PASSWORD, EMAIL) 
		VALUES 
		(#{name}, #{mobile}, #{hp}, #{zip_code}, #{address}, #{address2}, #{sex}, #{id}, #{password}, #{email} )
	</insert>

	
</mapper>
